package interactors.input_boundary;

public interface AccessPropertyInput {
    /**
     * Accesses the property associated with the given ID.
     *
     * @param PropertyID: ID of property to access.
     */
    void accessProperty(String PropertyID);
}
